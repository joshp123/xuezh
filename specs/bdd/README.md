# BDD scenarios

These scenarios define external behavior. Implementation should translate them into:
- unit tests (where possible)
- integration tests (CLI contract)
- end-to-end tests (flows)

Files are Gherkin-style `.feature` documents.
